vmauth:
  selectAllByDefault: false
  userSelector:
    matchLabels:
      auth: "true"

  ingress:
    enabled: true
    name: victoria-auth-ingress
    className: nginx
    host: vmauth.workquark.org
    annotations:
      nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
      nginx.ingress.kubernetes.io/rewrite-target: /
      nginx.ingress.kubernetes.io/ssl-redirect: "false"
      nginx.ingress.kubernetes.io/proxy-body-size: 100M
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
      external-dns.alpha.kubernetes.io/hostname: vmauth.workquark.org
      external-dns.alpha.kubernetes.io/target: 96d9d557-1bc6-4572-a608-a6330e6b5f90.cfargotunnel.com
    # Uncomment and configure for TLS
    # extraTls:
    #   - hosts:
    #       - vmauth.workquark.org
    #     secretName: vmauth-tls

vmusers:
# - name: admin
#   username: admin
#   password: password
#   targetRefs:
#   - static:
#       url: http://vlclusterlb-victoria-metrics-cluster.victoria-metrics-operator.svc:8427
#     paths:
#     - /

- name: reader
  username: reader
  password: password
  targetRefs:
  - static:
      urls:
      - http://vlclusterlb-victoria-logs-cluster-1.victoria-logs-cluster-1.svc:8427
      - http://vlclusterlb-victoria-logs-cluster-2.victoria-logs-cluster-2.svc:8427
    paths:
    - /
    - /select/vmui
    - /select/vmui/.*
    - /select/logsql/.*
